# AI 检索与回答规则

收到一个或多个产品问题后，必须先检索本仓库，再组织回答。不得使用未被仓库证据支持的产品参数、功能或结论。

## 仓库访问门槛

以下步骤是回答产品问题的前置条件，不得跳过：

1. 必须先成功读取本文件，再读取 `dataset/products.jsonl` 或 `dataset/series.jsonl` 确认检索范围。
2. 必须实际打开至少一个与问题对应的仓库数据文件，并取得手册名、PDF 页码或 `evidence_id`。仅看到用户提供的链接、仓库摘要或搜索结果，不算完成检索。
3. 完成上述步骤后，回答首行必须写：`仓库检索状态：已检索`。没有仓库内来源定位时，不得声称已经检索。
4. 如果当前 AI 没有联网、网页浏览、GitHub 或连接器访问能力，或者链接/文件读取失败，必须停止产品分析，只回复：`仓库检索状态：无法访问。当前无法读取指定仓库，请确认已启用网页、联网或 GitHub 访问能力后重试。` 不得改用模型自身知识回答。
5. 仓库已访问但未收录目标型号时，应明确说明未收录，不得使用仓库外知识补答。

## 问题路由

每个编号问题必须独立判断范围，不得继承上一问题的型号或系列。

1. 明确给出型号时，先在 `dataset/products.jsonl` 确认产品，再进入其 `dataset_directory` 检索。
2. 只给出蓝牙、Wi-Fi、LoRa、4G、LTE、GNSS、NFC、2.4G 等类别时，先查 `dataset/series.jsonl`，再进入 `dataset/by_series/` 对应目录。
3. 询问“哪款支持某功能”时，先查 `dataset/capabilities.jsonl`，再用候选记录的 `evidence_ids` 核对证据。
4. 未给出型号或类别时，先从 `products.jsonl` 和 `series.jsonl` 判断候选范围；仍有歧义时应说明并请求用户补充。
5. 跨型号结论仅限当前仓库 `products.jsonl` 已收录的产品，不得扩大到公司全部产品。

## 回答需求拆解

检索前必须先判断用户需要的是单值、范围、上限/下限、典型值、绝对最大额定值、列表还是条件判断，并列出答案必需字段。只有必需字段齐全，才算“足以回答”。

- “范围”“可用范围”“供电范围”：至少需要最小值、最大值、单位和适用条件；只有标称值或典型值时不得停止检索。
- “典型值”“标称值”：至少需要对应数值和单位，不得将其改写为范围。
- “最大允许值”“最低要求”：必须说明该值属于正常运行条件、推荐条件还是绝对最大额定值。
- “是否可以使用某值”：必须先取得正常运行范围及必要条件，再判断该值是否落在范围内。
- 列表或跨型号问题：必须覆盖当前问题范围内的所有适用记录，不能用第一条命中代表完整列表。

## 仓库结构

- `dataset/products.jsonl`：产品型号、别名、系列和分产品数据目录。
- `dataset/series.jsonl`：BT、WF、LR、CT、GP、NF、NR 系列及技术类别。
- `dataset/capabilities.jsonl`：跨型号功能候选索引。
- `dataset/by_product/`：已知型号时的首选检索入口。
- `dataset/by_series/`：只知道通信类型或系列范围时的首选入口。
- `dataset/catalog.jsonl`：手册、适用型号和手册目录。
- `manuals/`：各手册的 Markdown、图片、定位 PDF 和来源清单。
- `reports/`：构建质量报告，不能作为产品结论来源。

## 数据文件选择

- 参数、引脚、电气特性、频段和天线要求：`parameters.jsonl`
- AT 指令、格式、返回值或示例：`at_commands.jsonl`
- NMEA0183 语句：`nmea_sentences.jsonl`
- 一般说明、功能特点和应用流程：`chunks.jsonl`
- 精确原文和页码复核：`evidence.jsonl`
- PDF 逐页文本：`pages.jsonl`，仅在其他证据不足时使用

## 分层检索与快速路径

1. 根据问题范围，只检索对应产品或系列的主数据文件，并核对 `product_ids`、`applicable_models`、`series_id` 和 `source_locators`。
2. 用用户原词及必要的参数别名定向检索。例如供电类问题同时考虑“供电、工作电压、电源、VBAT、VIN、VCC、电气特性、推荐运行条件”。不要因第一条相近记录而停止。
3. 在当前产品或系列的同一主数据文件中，收集与目标参数直接相关的候选记录；区分概述、接口定义、电气特性、推荐运行条件和绝对最大额定值，不需要扫描无关数据文件。
4. 对照“回答需求拆解”检查字段完整性。单值问题命中完整且语义唯一的记录后可以走快速路径；范围问题只有同时取得最小值、最大值、单位和适用条件后才可以停止。
5. 最终采用的关键参数、存在歧义或冲突的记录，应通过对应 `evidence_ids` 复核原文、页码和适用型号。通常只复核支持最终结论的 1～2 条证据，不默认读取完整 `evidence.jsonl`。
6. 如果主文件返回被截断、定向搜索无结果但答案仍不完整，不能据此判断“仓库没有记录”；应进入对应手册的 `document.md`，按目录、章节名和参数名定位，再视需要查询 `pages.jsonl`。
7. `document.pdf` 不作为 AI 默认检索对象，仅供人工按页码复核。

不要同时扫描所有数据文件，也不要默认读取完整的全局 `evidence.jsonl`、`pages.jsonl`、`document.md` 或 `document.pdf`。只有答案不完整、语义有歧义、证据冲突或读取被截断时，才进入下一层检索。

## 数值证据语义优先级

回答正常供电范围、工作范围等问题时，按以下顺序选择证据，并保留原文条件：

1. 明确写明“供电范围”“工作范围”或“推荐运行条件”的原文。
2. 电气特性表中的最小值、典型值和最大值。
3. 电源接口表中的最小值、典型值和最大值。
4. 模块介绍或基础参数中的标称值、典型值。

“绝对最大额定值”表示器件不应超过的应力极限，不等于正常运行范围。除非用户明确询问绝对最大额定值，否则不得用它回答正常供电范围；即使数值上限相同，也必须区分二者语义。

## 功能支持判断

- `support_status: supported` 且存在明确证据时，可作为“支持”的候选结论；`review_status: candidate_unreviewed` 表示对外使用前仍建议人工抽查。
- `support_status: mentioned` 只表示资料提及该功能，不能直接判断产品支持。
- 没有找到功能记录或证据时只能回答“当前仓库无法确认”，不能判断为“不支持”。
- 如果资料明确写明不支持或存在条件，应保留否定语义和适用条件。

## 回答要求

- 回答首行必须给出仓库检索状态；随后给出结论状态：`明确`、`有条件` 或 `无法确认`。
- 明确说明检索范围和适用型号，不得把单一型号能力泛化到整个系列。
- 数值必须保留单位、条件、范围、典型值和最大/最小值语义。
- 最终回答前必须自检：问题要求的字段是否齐全、典型值是否被误写成范围、正常运行条件是否与绝对最大额定值混淆、同一参数是否还有直接相关记录、引用是否能直接支持结论。核心项目未通过时不得停止检索。
- AT 指令应尽量给出完整指令、用途、参数、返回值和注意事项；资料没有的字段不要补写。
- 多条证据不一致时，列出差异及各自来源，不自行选择一个值。
- `candidate_unreviewed` 是待复核状态，不等于资料无法确认。若已通过对应 `evidence_id` 核对原文、页码和适用型号，且证据完整无冲突，可以给出“明确”结论；尚未复核时应标为“有条件”。
- 找不到明确证据、只有模糊表述、适用型号不清或多条证据冲突时，结论状态必须为“有条件”或“无法确认”，并列出已检索范围。
- 无法可靠确认时，必须增加：`技术支持提示：当前仓库资料不足或存在歧义，无法可靠确认。建议联系公司技术支持，并提供产品型号、硬件/固件版本、应用场景及上述参考位置进行确认。`
- 不得虚构技术支持人员、联系方式或承诺；仓库未提供联系方式时，只提示用户通过公司既有渠道联系。
- 当前资料用于参数、特性、AT 指令、NMEA 和应用说明查询，不用于复杂故障诊断或安全承诺。

## 推荐回答格式

```text
仓库检索状态：已检索
结论状态：明确 / 有条件 / 无法确认

结论：……

检索范围：……
适用型号：……
说明：……

参考位置：
- 手册：……
- 章节/表格：……
- PDF：第 N 页（或第 N-M 页）
- 原文证据：……
- evidence_id：……

备注：候选抽取、条件限制或当前仓库收录范围。
技术支持提示：仅在有条件或无法确认时填写。
```

通过 `dataset/catalog.jsonl` 的 `manifest` 字段确定手册目录。页码优先取自主记录的 `source_locators`；进入证据复核层后，以 `evidence.jsonl` 的 `source_locator` 和 `quote` 为准。不要把搜索摘要当作原文。
